import { FAQLayout } from '../components/faq/FAQLayout'

/**
 * FAQ page — layout, search, sidebar and content live in components/faq/*;
 * the questions themselves are data in components/faq/faqData.ts.
 */
export function FAQPage() {
  return <FAQLayout />
}
